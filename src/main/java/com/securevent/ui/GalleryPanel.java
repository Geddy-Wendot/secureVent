package com.securevent.ui;

/**
 * Deprecated placeholder: GalleryPanel was part of an earlier UI draft.
 *
 * This file is intentionally minimal and marked deprecated to avoid
 * accidental use while the app uses `JournalPanel` as the primary UI.
 */
@Deprecated
public final class GalleryPanel {
    private GalleryPanel() { /* utility - not instantiable */ }
}
