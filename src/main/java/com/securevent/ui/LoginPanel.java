package com.securevent.ui;

/**
 * Deprecated placeholder: LoginPanel was part of an earlier UI draft.
 *
 * Kept as a minimal placeholder to avoid breaking references. The active
 * UI is `JournalPanel` so prefer that for new work.
 */
@Deprecated
public final class LoginPanel {
    private LoginPanel() { /* not instantiable */ }
}
